package dev.timkante.badgeK.spotify.components.styles

import kotlinx.css.LinearDimension
import kotlinx.css.px

enum class TextSize(val size: LinearDimension) {
    LARGE(16.px),
    DEFAULT(14.px),
    SMALL(12.px);
}